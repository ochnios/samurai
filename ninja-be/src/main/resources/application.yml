spring:
  profiles:
    default: dev
    active: ${SPRING_ACTIVE_PROFILE}

springdoc:
  swagger-ui:
    path: /swagger-ui
    doc-expansion: none
  api-docs:
    resolve-schema-properties: true
  packages-to-scan: pl.ochnios.ninjabe
  default-produces-media-type: application/json